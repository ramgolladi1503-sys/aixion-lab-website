import { test, expect } from "@playwright/test";
test("home matches the approved editorial portfolio composition", async ({ page }) => { await page.goto("/home", { waitUntil: "networkidle" }); await expect(page.getByRole("heading", { name: "From complex problems to enduring progress." })).toBeVisible(); await expect(page.locator(".editorial-home")).toBeVisible(); await expect(page.locator(".mockup-home-diagram")).toBeVisible(); await expect(page.locator(".home-system-row")).toHaveCount(4); });
test("home remains readable under reduced motion", async ({ page }) => { await page.emulateMedia({ reducedMotion: "reduce" }); await page.goto("/home", { waitUntil: "networkidle" }); await expect(page.locator("main#main-content")).toBeVisible(); await expect(page.getByRole("link", { name: /Explore our work/i })).toBeVisible(); expect(await page.evaluate(() => document.documentElement.scrollWidth <= window.innerWidth + 1)).toBe(true); });
test("entry remains separate from editorial home", async ({ page }) => { await page.goto("/", { waitUntil: "networkidle" }); await expect(page.locator(".unseen-hero-container")).toBeVisible(); await expect(page.locator(".editorial-home")).toHaveCount(0); });
