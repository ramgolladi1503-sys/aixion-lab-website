name: Visual Evidence

on:
  pull_request:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read

jobs:
  capture-and-validate:
    runs-on: ubuntu-latest
    timeout-minutes: 12
    defaults:
      run:
        working-directory: frontend
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node 24
        uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: npm
          cache-dependency-path: frontend/package-lock.json

      - name: Install deterministic dependencies
        run: npm ci

      - name: Install Chromium
        timeout-minutes: 4
        run: npx playwright install --with-deps chromium

      - name: Build production bundle
        run: npm run build

      - name: Capture normal-motion visual matrix
        env:
          VISUAL_RUN_ID: ${{ github.sha }}
        run: npm run visual:capture

      - name: Validate normal-motion choreography
        run: npm run test:motion

      - name: Upload visual evidence
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: aixion-visual-evidence-${{ github.sha }}
          path: |
            frontend/test-results/visual-evidence
            frontend/playwright-report
            frontend/test-results/playwright
          if-no-files-found: warn
          retention-days: 14
